# 4학년 교과 어휘 학습 앱

초등학교 4학년 학생이 **국어 · 사회 · 과학** 교과서의 핵심 어휘를
플래시카드 → 퀴즈 → 오답 복습 흐름으로 익히는 학습 웹앱입니다.

React + Vite + TypeScript로 만들었고, 외부 UI 라이브러리 없이 표준 웹 기술
(localStorage, Web Speech API)만 사용합니다.

---

## 1. 실행 방법

필요 도구: [Node.js](https://nodejs.org) 18 이상

```bash
npm install      # 의존성 설치 (처음 한 번)
npm run dev      # 개발 서버 실행 → 안내된 http://localhost:5173 접속
npm run build    # 배포용 빌드 (dist/ 폴더 생성)
npm run preview  # 빌드 결과 미리보기
```

---

## 2. 주요 기능

| 모드 | 설명 |
|---|---|
| 📇 어휘 카드 | 앞면(어휘·교과·단원·이모지)을 보고 누르면 뒷면(쉬운 뜻·예문)으로 뒤집힘. 이전/다음 이동, 소리로 듣기. 상단에 **오늘의 어휘** 표시 |
| 📝 퀴즈 풀기 | 한 번에 최대 5문제. **뜻 맞히기**와 **문장 빈칸 채우기** 두 유형이 무작위로 출제. 보기 4개(정답1+오답3), 즉시 정답/오답 피드백, 마지막에 결과·정답률 화면 |
| 🔁 오답 복습 | 퀴즈에서 틀린 어휘만 모아서 복습. 소리로 듣기 / 복습 완료 버튼 |

- **교과 필터**: 전체 / 국어 / 사회 / 과학 선택 가능 (카드·퀴즈·상태표시에 반영)
- **상태 표시줄**: 선택 교과, 학습 어휘 수, 저장된 오답 수, 누적 정답률
- **TTS(소리로 듣기)**: `[어휘]. [쉬운 뜻]. 예문. [예문]` 순서로 한국어 음성 재생.
  음성 미지원 브라우저에서는 안내 문구 표시
- **빈칸 채우기**: 보기를 클릭하거나 빈칸 위로 드래그 앤 드롭해서 답할 수 있음
- **반응형**: 데스크톱·태블릿·스마트폰 모두 지원 (모바일에서 2열 → 1열)

---

## 3. 교사가 어휘 데이터를 수정하는 방법

모든 어휘는 한 파일에 모여 있습니다.

> 📄 `src/data/vocab.ts` 의 **`VOCAB_DB`** 배열

어휘 하나를 아래 형태로 추가/수정/삭제하면 됩니다.

```ts
{
  id: "so-10",                                   // 겹치지 않는 고유 값
  word: "축제",                                  // 어휘
  subject: "사회",                               // "국어" | "사회" | "과학"
  unit: "우리 지역의 문화",                       // 단원 이름
  easy_meaning: "여러 사람이 모여 즐기는 행사",   // 쉬운 뜻
  example_sentence: "우리 지역에서는 해마다 봄에 축제가 열려요.", // 예문
  image_url: "🎉",                               // 이모지 1개
}
```

⚠️ **중요**: `example_sentence`(예문) 안에는 `word`(어휘)가 글자 그대로 반드시
들어가야 합니다. 빈칸 채우기 퀴즈가 예문에서 어휘를 찾아 빈칸으로 바꾸기 때문입니다.

파일을 저장하면 개발 서버가 자동으로 화면을 새로 고칩니다. 별도 빌드 없이도 반영됩니다.

---

## 4. 오답 저장 방식

브라우저의 **localStorage**에 저장되어 새로고침·재방문 후에도 유지됩니다.

| 저장 키 | 내용 |
|---|---|
| `grade4_vocab_wrong_words_v1` | 틀린 어휘의 `id` 목록 |
| `grade4_vocab_stats_v1` | 누적 시도 수 / 정답 수 (정답률 계산용) |

저장 규칙:

- 퀴즈에서 **틀리면** 해당 어휘를 오답 목록에 추가 (같은 어휘는 중복 저장 안 함)
- 퀴즈에서 **다시 맞히면** 오답 목록에서 제거
- 오답 복습 화면에서 **복습 완료**를 누르면 제거

> 참고: localStorage는 사용 기기·브라우저별로 따로 저장됩니다. 학생마다 자신의
> 기기에서 자신의 오답을 관리하게 됩니다.

---

## 5. 배포 방법 (Vercel)

이 앱은 백엔드·환경변수가 없는 정적 SPA라서 Vercel에 바로 올릴 수 있습니다.
저장소에 포함된 `vercel.json`이 새로고침 시에도 정상 동작하도록 설정합니다.

### 방법 A — Vercel CLI

```bash
npm i -g vercel     # Vercel CLI 설치 (처음 한 번)
vercel login        # 브라우저로 로그인 (처음 한 번)
vercel              # 미리보기 배포 (Build Command: vite build / Output: dist 자동 인식)
vercel --prod       # 운영(공개) 배포
```

### 방법 B — GitHub 연동 (자동 배포)

1. 이 폴더를 GitHub 저장소에 올립니다.
2. [vercel.com](https://vercel.com)에서 **Add New → Project → Import**로 저장소 선택.
3. 프레임워크가 **Vite**로 자동 감지됩니다. 그대로 **Deploy**.
4. 이후 GitHub에 푸시할 때마다 자동으로 재배포됩니다.

> 배포 후 받은 공개 URL을 여기에 적어 두세요: `https://<your-app>.vercel.app`

---

## 6. 확장 아이디어

기본 구조가 단순해서 다음 기능을 덧붙이기 쉽습니다.

- 단원별 필터 (현재 `unit` 필드 활용)
- 즐겨찾기 어휘 / 학습 완료 배지
- 교사용 어휘 추가 화면, CSV·Excel 불러오기
- 난이도별 어휘 분류, 학급별 오답 빈도 분석
- 정답률 통계 상세 화면 (현재는 누적 정답률만 표시)

---

## 7. 폴더 구조

```
src/
  data/vocab.ts        ← 어휘 데이터 (교사가 수정하는 곳)
  types.ts             공용 타입
  hooks/               useWrongWords(오답) · useStats(통계) · useTTS(음성)
  utils/shuffle.ts     보기 섞기 · 퀴즈 생성 · 빈칸 문장 만들기
  components/          Header · SubjectFilter · ModeTabs · StatusBar
                       Flashcard · WordOfTheDay · Quiz · QuizResult · ReviewNotes
  styles/global.css    색상 토큰 · 반응형 · 접근성
  App.tsx              전체 화면 조립
```
